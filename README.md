# Laniakea

`Laniakea` is a GitOps-native infrastructure-as-code (IaC) tool.

It uses `scopes` to describe infrastructure boundaries such as bare
metal servers, `kubernetes` clusters, service meshes, tenants, and
applications.
`Laniakea` loads those declarations, validates them, and reconciles
them through GitOps workflows.
